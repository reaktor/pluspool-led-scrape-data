{
  "name": "pluspool-led-scrape-data",
  "version": "1.2.0",
  "description": "A node app that pulls in noaa api and a datagarrison stream to create a unified data file and uploads to S3.",
  "private": true,
  "main": "index.js",
  "scripts": {
    "lint": "standard",
    "lint:fix": "standard --fix",
    "test": "jest"
  },
  "devDependencies": {
    "@jedahan/predicted-mpn": "git+ssh://git@github.com/jedahan/predicted-mpn.git",
    "aws-sdk": "^2.524.0",
    "datagarrison": "^2.0.0",
    "dotenv": "^8.1.0",
    "isomorphic-unfetch": "^3.0.0",
    "jest": "^24.9.0",
    "pako": "^1.0.10",
    "standard": "^14.1.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
