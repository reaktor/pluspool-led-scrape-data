{
  "name": "data-process",
  "version": "1.0.0",
  "description": "A node app that pulls in noaa api and a datagarrison stream to create a unified data file and uploads to S3.",
  "private": true,
  "main": "index.js",
  "scripts": {
    "lint": "standard",
    "lint:fix": "standard --fix",
    "test": "jest"
  },
  "devDependencies": {
    "aws-sdk": "^2.488.0",
    "datagarrison": "^0.1.0",
    "dotenv": "^8.0.0",
    "isomorphic-unfetch": "^3.0.0",
    "jest": "^24.8.0",
    "standard": "^12.0.1"
  },
  "standard": {
    "env": [
      "jest"
    ]
  }
}
